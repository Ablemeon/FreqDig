# 变更日志

本文件记录项目的重要变更。

格式大致参考 Keep a Changelog。准备发布版本时，可以将 `未发布` 中的内容移动到对应的带日期版本段落中。

[English](./CHANGELOG.md) | 中文

## [未发布]

### 新增

- 新增 REW THD 文本文件导入支持。
- 新增失真模式，可绘制 THD、Noise 与各阶谐波失真曲线。
- 新增失真数据分组管理，支持重命名、移除和每条曲线独立显隐。
- 新增多组失真数据对比分析，启用多组数据时自动切换为组间对比。
- 新增失真分析计算范围：
  - 全频段 20 Hz - 20 kHz
  - 可信频段 100 Hz - 2 kHz
  - 可信宽频 100 Hz - 10 kHz
  - 超低频 20 Hz - 60 Hz
  - 低频 60 Hz - 300 Hz
  - 超高频 10 kHz - 20 kHz
- 新增 PNG 和 SVG 导出时附加失真分析。
- 新增曲线/失真双态模式切换开关。
- 新增失真模式三栏布局。

### 变更

- 失真模式复用图表 tooltip、悬停高亮、图例高亮、缩放和频带控制能力。
- 失真纵轴模式简化为百分比和 dBr。
- 百分比模式纵轴上限固定为 100%。
- dBr 模式纵轴上限固定为 0 dBr。
- 右侧纵轴显示与左侧互补的失真单位。
- 新导入失真数据默认隐藏 H1。
- 精简失真模式下的 header 控件，减少误操作。
- 优化暗色模式下失真控件的视觉风格。

### 重构

- 将失真计算和格式化逻辑移动到 `src/distortion.js`。
- 将失真侧栏控件移动到 `src/distortionPanel.js`。
- 将主题和暗色模式切换控制移动到 `src/themeController.js`。

### 移除

- 移除废弃的 `animate-demo.html` 页面。

## [0.0.1] - 2026-04-30

### 新增

- 初始本地频响分析流程。
