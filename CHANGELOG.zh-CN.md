# 变更日志

本文件记录项目的重要变更。

格式大致遵循 Keep a Changelog。准备发布版本时，可以将 `Unreleased` 中的内容移动到带日期的版本段落中。

[English](./CHANGELOG.md) | 中文

## [Unreleased]

### 变更

- 开源版界面、导入和工程流程仅保留频响、失真、群延迟三个分析功能。

### 新增

- 新增 REW THD 文本导入支持。
- 新增失真模式，可查看 THD、Noise 和各阶谐波失真曲线。
- 新增失真数据分组管理，支持重命名、移除、显示隐藏和单曲线控制。
- 新增失真分析范围和失真分析导出。
- 新增 REW Impulse Response 导入，用于 IR 分析和瀑布图分析。
- 新增由相位斜率计算的群延迟分析。
- 新增 WebGL2 三维瀑布图渲染器，支持切片绘制和网格曲面。
- 新增瀑布图切片数量、动态范围、时间深度、缩放、旋转、平移、频率宽度和算法控制。
- 新增瀑布图分析摘要，并与当前瀑布图设置联动。
- 新增频响/相位曲线的 WebGL2 加速绘制。
- 为主要 JavaScript 文件新增定位型注释，方便查找功能入口。

### 变更

- 频响曲线绘制改为混合路径：WebGL2 负责高密度曲线描边，Canvas2D 继续负责坐标、文字、tooltip、图例和导出合成。
- 倍频程平滑改为对数频率加权平滑，减少窄窗口下的锯齿感。
- 相对瀑布图数据保留到 `-80 dB`，默认动态范围加宽。
- 瀑布图切片数量最高可生成 `501` 帧。
- 瀑布图默认视角改为旋转 `-20°`、俯仰 `0°`。
- 优化暗色模式和各图表模式下的 header/图层表现。

### 修复

- 修复瀑布图 PNG/SVG 导出合成路径。
- 修复暗色模式下部分 2D 图表不可见的问题。
- 通过 WebGL2 切片/网格渲染器替换早期连续曲面，修复多个瀑布图遮挡和灰色面片问题。
- 修复倍频程平滑后曲线可能比原始曲线更锯齿的问题。

### 重构

- 将失真计算和格式化逻辑移动到 `src/distortion.js`。
- 将失真侧栏控制移动到 `src/distortionPanel.js`。
- 将主题和暗色模式切换控制移动到 `src/themeController.js`。
- 新增 `src/waterfall.js`、`src/waterfall3d.js`、`src/impulseAnalysis.js`、`src/groupDelay.js` 和 `src/frequencyWebgl.js` 等分析与渲染模块。

### 移除

- 移除过时的 `animate-demo.html` 页面。

## [0.0.1] - 2026-04-30

### 新增

- 初始本地频响分析流程。
